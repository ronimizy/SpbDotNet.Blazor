@page "/counter"
@rendermode InteractiveServer

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p>
    This counter consists of two components.<br/>
    One is parent, other is inner.<br/>
    Each of them can increment the value, but increments of inner component does not apply to parent, 
    as there is no two-way binding between the values 
</p>

<CompositeCounterComponent/>